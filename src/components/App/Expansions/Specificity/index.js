import React from 'react';
import PropTypes from 'prop-types';

import './style.scss';

const Specificity = () => (
  <div className="specificity">Specificity</div>
);

Specificity.propTypes = {

};

export default Specificity;
